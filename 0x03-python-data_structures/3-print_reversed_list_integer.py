#!/usr/bin/python3
def print_reversed_list_integer(my_list=[]):
    if my_list = None:
        return None
    i, length = -1, len(my_list)
    while length != 0:
        print("{:d}".format(my_list[i]))
        i -= 1
        length -= 1
